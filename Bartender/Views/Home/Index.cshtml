@model Bartender.Models.DrinkMenu
@{
    ViewData["Title"] = "Home Page";
}
<div class="container">
    <table class="table table-bordered" style="margin-top:50px">
        <tr>
            <th>Drink Name</th>
            <th>Alcohol Content</th>
            <th>Price</th>
            <th>Description</th>
            <th>Request Drink</th>
        </tr>
        @if(Model.Beverages.Count > 0) { 
        @for (int i = 0; i < Model.Beverages.Count; i++)
        {
        <tr>
            <td>@Model.Beverages[i].Name</td>
            <td>@Model.Beverages[i].AlcContent</td>
            <td>$ @Model.Beverages[i].Cost</td>
            <td>
                <button onclick="viewDescription()" id="desc" value="@Model.Beverages[i].Description" type="button" class="btn btn-secondary" data-toggle="modal" data-target="#viewDescription">Request Drink</button>
            </td>            
            <td>
                <button type="button" class="btn btn-secondary" data-toggle="modal" data-target="#requestDrink">Request Drink</button>
            </td>

        </tr>
                }
                }
        </table>
</div>

<!-- Request Modal -->
<div class="modal fade" id="requestDrink" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">Request Drink</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                <form>
                    <div class="form-group">
                        <input id="requestorName" type="text" class="form-control" placeholder="Enter Name">
                    </div>                   
                    <div class="form-group">
                        <select id="drinkQuantity" onchange="drinkQuant()">
                            <option>Select Quantity</option>
                            @for (int i = 1; i < 11; i++)
                            {
                                <option value="@i">@i Drink(s)</option>

                            }
                        </select>
                    </div>
                    <div class="form-group">
                        <table class="table table-bordered" style="margin-top:10px">
                            <tr>
                                <th>Drink Cost</th>
                                <th>Quantity</th>
                                <th>Total</th>
                            </tr>
                        </table>
                    </div>
                </form>
            </div>
            <div class="modal-footer">
                <button class="btn btn-secondary" onclick="requestDrink()">Request</button>
                <button class="btn btn-secondary" type="button" data-dismiss="modal">Close</button>
            </div>
        </div>
    </div>
</div>

<!-- Description Modal -->
<div class="modal fade" id="viewDescription" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true" >
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">Drink Details</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <p id="description"></p>
            <div class="modal-footer">
                <button class="btn btn-secondary" type="button" data-dismiss="modal">Close</button>
            </div>
        </div>
    </div>
</div>

<script type="text/javascript">
    $('#requestDrink').on('shown.bs.modal', function () {
    });
    $('#viewDescription').on('shown.bs.modal', function () {
    });
    function drinkQuant() {
        var quantity = parseInt(document.getElementById("drinkQuantity").value);
        alert(quantity);
    }
    function requestDrink() {

    }
    function viewDescription() {
        document.getElementById("description").innerText = document.getElementById("desc").value;
    }
</script>